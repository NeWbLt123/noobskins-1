var express = require("express");
var Promise = require('bluebird');

module.exports = function(steamService) {
    var router = express.Router();

    // API METHODS FOR STEAM GO HERE

    return router;
}
